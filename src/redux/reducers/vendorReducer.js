const initialState = {
  vendors: [],
};

const vendorReducer = (state = initialState, action) => {
  switch (action.type) {
    // Add cases as needed
    default:
      return state;
  }
};

export default vendorReducer;
